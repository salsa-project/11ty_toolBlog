<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ seo.description }}">
    <meta property="og:title" content="{{ seo.title or title }}">
    <meta property="og:description" content="{{ seo.description }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ url }}">
    <meta property="og:image" content="{{ seo.image }}">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ seo.title or title }}">
    <meta name="twitter:description" content="{{ seo.description }}">
    <meta name="twitter:image" content="{{ seo.image }}">
    <title>{{ seo.title or title }}  | {{ meta.siteName }}</title>
    <link rel="stylesheet" href="/css/styles.css"/>
    <link rel="alternate" type="application/atom+xml" title="RSS Feed for {{ meta.siteName }}" href="/feed.xml"/>
    {# Only include the Flickity js if this page uses a hero.carousel  #}
    {% if hero == "carousel" or product.carousel -%}
      <link rel="stylesheet" href="https://unpkg.com/flickity@2/dist/flickity.min.css">
      <link rel="stylesheet" href="https://unpkg.com/flickity-fullscreen@1/fullscreen.css">
    {%- endif -%}
    {%- include "partials/head.njk" %}
    {% set prismOkaidia %}{% include "src/assets/css/prism-okaidia.css" %}{% endset -%}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lexend:wght@100..900&family=Onest:wght@100..900&display=swap" rel="stylesheet">
    <style>
      {{prismOkaidia | safe}}
    </style>
  </head>
  <body class="{{ colors.siteTextCustom or siteTextDefault }} bg-thirdColor">
    {% svgsprite %}
      {% include "components/nav.njk" %}
    <div class="flex flex-col {{structure.g_customPageWidth}} mt-12 p-6 sm:p-0" style="min-height: calc(100vh - 48px);">

      {#  Allows a user to easily change the Hero type on the Home page...or any page.
          See /src/home.md for how to use. 
      #}
      {% if hero %}
        {% set heroComponent = ['components/hero-', hero, '.njk'] %}
        {% include heroComponent | join %}
      {% endif %}

      {# If any frontmatter includes images.feature, insert it at the top of the page, 
      below the nav and outside the default wrapper below. #}
      {% if images.feature %}
        <figure class="sm:w-screen md:w-full mx-auto {{ images.height or "h-80 md:h-8/10" }} ">
          <img class="w-full h-full md:rounded-xl" src="{{ images.feature if 'http' in images.feature else "/assets/images/" + images.feature }}" alt="{{ title }}">
        </figure>
      {% endif %}

      <div class="flex-grow w-full">
        <main class="block py-12" id="content">
          {{ content | safe }}
        </main>
      </div>

      {% include "partials/footer.njk" %}
    </div>

    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

    {# Only include the Flickity js if this page uses a hero.carousel  #}
    {% if hero == "carousel" or product.carousel %}
      <script src="https://unpkg.com/flickity@2/dist/flickity.pkgd.min.js"></script>
      <script src="https://unpkg.com/flickity-fullscreen@1/fullscreen.js"></script>
    {% endif %}

  </body>
</html>